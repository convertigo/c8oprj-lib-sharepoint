accessibility: Hidden
authenticatedContextRequired: true
comment: Updates one SharePoint list item through Microsoft Graph (online) or SharePoint REST (on-prem). (Graph permissions Sites.ReadWrite.All)
↓inputVars [steps.InputVariablesStep-1772620911001]: 
  comment: Exposes request inputs in XML for debugging and source mapping.
↓ExecuteUpdateListItem [steps.SimpleStep-1772620911002]: 
  comment: Resolves site and list then updates list item fields through provider-specific API.
  expression: |
    'include("js/sharepoint_provider.js");
    
    var gs_isBlank = function(value) {
    	return value === null || value === undefined || String(value).trim().length === 0;
    };
    
    var gs_safeString = function(value) {
    	return value === null || value === undefined ? "" : String(value);
    };
    
    var gs_defaultString = function(value, defaultValue) {
    	return gs_isBlank(value) ? defaultValue : String(value);
    };
    
    var gs_require = function(name, value) {
    	if (gs_isBlank(value)) {
    		throw new java.lang.IllegalArgumentException("Missing required input: " + name);
    	}
    	return String(value);
    };
    
    var gs_toBoolean = function(value, defaultValue) {
    	if (value === null || value === undefined || String(value).trim().length === 0) {
    		return defaultValue === true;
    	}
    	var normalized = String(value).toLowerCase();
    	return normalized === "true" || normalized === "1" || normalized === "yes" || normalized === "y";
    };
    
    var gs_errorClass = function(error) {
    	try {
    		if (error && error.javaException) {
    			return String(error.javaException.getClass().getName());
    		}
    		if (error && error.getClass) {
    			return String(error.getClass().getName());
    		}
    	} catch (ignore) {
    	}
    	return "";
    };
    
    var gs_responseOk = function(graphOperation, tokenMode, requiredPermission, data) {
    	return {
    		ok: true,
    		status: "OK",
    		graphOperation: graphOperation,
    		tokenMode: tokenMode,
    		requiredPermission: requiredPermission,
    		data: data
    	};
    };
    
    var gs_responseError = function(graphOperation, tokenMode, requiredPermission, error) {
    	var message = "";
    	try {
    		message = String(error.message);
    	} catch (ignoreMessage) {
    		message = String(error);
    	}
    	return {
    		ok: false,
    		status: "ERROR",
    		graphOperation: graphOperation,
    		tokenMode: tokenMode,
    		requiredPermission: requiredPermission,
    		errorClass: gs_errorClass(error),
    		errorMessage: message
    	};
    };
    
    var gs_readAll = function(inputStream) {
    	if (inputStream === null) {
    		return "";
    	}
    	var scanner = new java.util.Scanner(inputStream, "UTF-8").useDelimiter("\\A");
    	try {
    		return scanner.hasNext() ? String(scanner.next()) : "";
    	} finally {
    		try {
    			scanner.close();
    		} catch (ignoreScanner) {
    		}
    	}
    };
    
    var gs_urlEncode = function(value) {
    	return java.net.URLEncoder.encode(String(value), "UTF-8");
    };
    
    var gs_encodePathSegment = function(value) {
    	return String(gs_urlEncode(value)).replace(/\+/g, "%20");
    };
    
    var gs_appendQueryParam = function(url, name, value) {
    	if (gs_isBlank(value)) {
    		return url;
    	}
    	var separator = url.indexOf("?") >= 0 ? "&" : "?";
    	return url + separator + gs_encodePathSegment(name) + "=" + gs_encodePathSegment(value);
    };
    
    var gs_httpRequest = function(method, endpoint, bearerToken, payload, contentType, accept) {
    	var url = new java.net.URL(String(endpoint));
    	var connection = url.openConnection();
    	var normalizedMethod = String(method).toUpperCase();
    	var methodToUse = normalizedMethod;
    	var patchOverride = false;
    
    	try {
    		connection.setRequestMethod(methodToUse);
    	} catch (methodError) {
    		if (normalizedMethod === "PATCH") {
    			methodToUse = "POST";
    			patchOverride = true;
    			connection.setRequestMethod(methodToUse);
    		} else {
    			throw methodError;
    		}
    	}
    
    	connection.setDoInput(true);
    	connection.setUseCaches(false);
    
    	if (!gs_isBlank(accept)) {
    		connection.setRequestProperty("Accept", String(accept));
    	}
    	if (!gs_isBlank(bearerToken)) {
    		connection.setRequestProperty("Authorization", "Bearer " + String(bearerToken));
    	}
    	if (patchOverride) {
    		connection.setRequestProperty("X-HTTP-Method-Override", "PATCH");
    	}
    
    	if (payload !== null && payload !== undefined) {
    		connection.setDoOutput(true);
    		if (!gs_isBlank(contentType)) {
    			connection.setRequestProperty("Content-Type", String(contentType));
    		}
    		var output = connection.getOutputStream();
    		try {
    			output.write(new java.lang.String(String(payload)).getBytes("UTF-8"));
    		} finally {
    			output.close();
    		}
    	}
    
    	var statusCode = connection.getResponseCode();
    	var body = gs_readAll(statusCode >= 200 && statusCode < 300 ? connection.getInputStream() : connection.getErrorStream());
    	return {
    		statusCode: statusCode,
    		body: body
    	};
    };
    
    var gs_graphGetJson = function(endpoint, token) {
    	var result = gs_httpRequest("GET", endpoint, token, null, "", "application/json");
    	if (result.statusCode < 200 || result.statusCode >= 300) {
    		throw new java.lang.RuntimeException("Graph request failed (" + result.statusCode + "): " + result.body);
    	}
    	if (gs_isBlank(result.body)) {
    		return {};
    	}
    	return JSON.parse(result.body);
    };
    
    var gs_graphPatchJson = function(endpoint, token, payloadObject) {
    	var payloadString = JSON.stringify(payloadObject);
    	var requestBuilder = java.net.http.HttpRequest.newBuilder(java.net.URI.create(String(endpoint)));
    	requestBuilder.header("Accept", "application/json");
    	requestBuilder.header("Content-Type", "application/json");
    	if (!gs_isBlank(token)) {
    		requestBuilder.header("Authorization", "Bearer " + String(token));
    	}
    	requestBuilder.method("PATCH", java.net.http.HttpRequest.BodyPublishers.ofString(payloadString));
    
    	var response = java.net.http.HttpClient
    		.newBuilder()
    		.followRedirects(java.net.http.HttpClient.Redirect.NORMAL)
    		.build()
    		.send(requestBuilder.build(), java.net.http.HttpResponse.BodyHandlers.ofString());
    
    	var statusCode = response.statusCode();
    	var body = gs_safeString(response.body());
    	if (statusCode < 200 || statusCode >= 300) {
    		throw new java.lang.RuntimeException("Graph patch failed (" + statusCode + "): " + body);
    	}
    	if (gs_isBlank(body)) {
    		return {};
    	}
    	return JSON.parse(body);
    };
    
    var gs_acquireAppToken = function(tenantIdValue, clientIdValue, clientSecretValue, scopeValue) {
    	var endpoint = "https://login.microsoftonline.com/" + gs_require("tenantId", tenantIdValue) + "/oauth2/v2.0/token";
    	var payload =
    		"client_id=" + gs_urlEncode(gs_require("clientId", clientIdValue)) +
    		"&client_secret=" + gs_urlEncode(gs_require("clientSecret", clientSecretValue)) +
    		"&scope=" + gs_urlEncode(gs_defaultString(scopeValue, "https://graph.microsoft.com/.default")) +
    		"&grant_type=client_credentials";
    
    	var tokenResponse = gs_httpRequest("POST", endpoint, "", payload, "application/x-www-form-urlencoded", "application/json");
    	if (tokenResponse.statusCode < 200 || tokenResponse.statusCode >= 300) {
    		throw new java.lang.RuntimeException("Token endpoint failed (" + tokenResponse.statusCode + "): " + tokenResponse.body);
    	}
    
    	var parsed = JSON.parse(tokenResponse.body);
    	if (!parsed || gs_isBlank(parsed.access_token)) {
    		throw new java.lang.RuntimeException("Token endpoint returned no access_token");
    	}
    
    	return {
    		mode: "application",
    		token: String(parsed.access_token),
    		tokenType: gs_safeString(parsed.token_type),
    		expiresIn: parsed.expires_in === undefined ? 0 : parsed.expires_in,
    		scope: gs_safeString(parsed.scope)
    	};
    };
    
    var gs_resolveToken = function(accessTokenValue, tenantIdValue, clientIdValue, clientSecretValue) {
    	if (!gs_isBlank(accessTokenValue)) {
    		return {
    			mode: "delegated",
    			token: String(accessTokenValue),
    			tokenType: "Bearer",
    			expiresIn: 0,
    			scope: ""
    		};
    	}
    	return gs_acquireAppToken(tenantIdValue, clientIdValue, clientSecretValue, "https://graph.microsoft.com/.default");
    };
    
    var gs_normalizeSitePath = function(sitePathValue) {
    	var rawPath = gs_require("sitePath", sitePathValue).trim();
    	if (rawPath.length === 0) {
    		throw new java.lang.IllegalArgumentException("sitePath cannot be empty");
    	}
    	if (rawPath.charAt(0) !== "/") {
    		rawPath = "/" + rawPath;
    	}
    	if (rawPath.length > 1 && rawPath.charAt(rawPath.length - 1) === "/") {
    		rawPath = rawPath.substring(0, rawPath.length - 1);
    	}
    	return rawPath;
    };
    
    var gs_encodeSitePath = function(sitePathValue) {
    	if (sitePathValue === "/") {
    		return "/";
    	}
    	var parts = String(sitePathValue).split("/");
    	var encodedParts = [];
    	var i;
    	for (i = 0; i < parts.length; i++) {
    		if (parts[i].length > 0) {
    			encodedParts.push(gs_encodePathSegment(parts[i]));
    		}
    	}
    	return "/" + encodedParts.join("/");
    };
    
    var gs_odataEscapeString = function(value) {
    	return String(value).split("''").join("''");
    };
    
    var gs_splitCsv = function(csvValue) {
    	if (gs_isBlank(csvValue)) {
    		return [];
    	}
    	var rawParts = String(csvValue).split(",");
    	var normalized = [];
    	var i;
    	for (i = 0; i < rawParts.length; i++) {
    		var trimmed = String(rawParts[i]).trim();
    		if (trimmed.length > 0) {
    			normalized.push(trimmed);
    		}
    	}
    	return normalized;
    };
    
    var gs_parseJsonObject = function(name, rawJson) {
    	var source = gs_require(name, rawJson);
    	var parsed = null;
    	try {
    		parsed = JSON.parse(String(source));
    	} catch (parseError) {
    		throw new java.lang.IllegalArgumentException("Invalid JSON for " + name + ": " + gs_safeString(parseError.message));
    	}
    	if (parsed === null || parsed === undefined || parsed instanceof Array || typeof parsed !== "object") {
    		throw new java.lang.IllegalArgumentException(name + " must be a JSON object");
    	}
    	return parsed;
    };
    
    var gs_resolveSiteAndList = function(token, siteIdValue, siteHostnameValue, sitePathValue, listIdValue, listNameValue) {
    	var resolvedSiteId = gs_safeString(siteIdValue);
    	var resolvedSiteHostname = "";
    	var resolvedSitePath = "";
    
    	if (gs_isBlank(resolvedSiteId)) {
    		resolvedSiteHostname = gs_require("siteHostname", siteHostnameValue).toLowerCase();
    		resolvedSitePath = gs_normalizeSitePath(sitePathValue);
    		var siteEndpoint =
    			"https://graph.microsoft.com/v1.0/sites/" +
    			resolvedSiteHostname +
    			":" +
    			gs_encodeSitePath(resolvedSitePath) +
    			"?$select=id,webUrl,name,displayName";
    		var siteObject = gs_graphGetJson(siteEndpoint, token);
    		resolvedSiteId = gs_require("siteId", siteObject.id);
    	}
    
    	var resolvedListId = gs_safeString(listIdValue);
    	var requestedListName = gs_safeString(listNameValue);
    	if (gs_isBlank(resolvedListId) && gs_isBlank(requestedListName)) {
    		throw new java.lang.IllegalArgumentException("Provide listId or listName");
    	}
    
    	var listObject = null;
    	if (!gs_isBlank(resolvedListId)) {
    		var byIdEndpoint =
    			"https://graph.microsoft.com/v1.0/sites/" + gs_encodePathSegment(resolvedSiteId) +
    			"/lists/" + gs_encodePathSegment(resolvedListId) +
    			"?$select=id,name,displayName,webUrl,list,createdDateTime,lastModifiedDateTime,sharepointIds";
    		listObject = gs_graphGetJson(byIdEndpoint, token);
    	} else {
    		var queryEndpoint =
    			"https://graph.microsoft.com/v1.0/sites/" + gs_encodePathSegment(resolvedSiteId) +
    			"/lists?$select=id,name,displayName,webUrl,list,createdDateTime,lastModifiedDateTime,sharepointIds";
    		var lookup = gs_odataEscapeString(requestedListName);
    		queryEndpoint = gs_appendQueryParam(queryEndpoint, "$filter", "displayName eq ''" + lookup + "''");
    		var queryObject = gs_graphGetJson(queryEndpoint, token);
    		if (!(queryObject.value instanceof Array) || queryObject.value.length === 0) {
    			throw new java.lang.RuntimeException("List not found: " + requestedListName);
    		}
    		listObject = queryObject.value[0];
    		resolvedListId = gs_safeString(listObject.id);
    	}
    
    	return {
    		siteId: resolvedSiteId,
    		siteHostname: resolvedSiteHostname,
    		sitePath: resolvedSitePath,
    		listId: gs_safeString(resolvedListId),
    		listName: gs_safeString(listObject.name),
    		listDisplayName: gs_safeString(listObject.displayName),
    		listWebUrl: gs_safeString(listObject.webUrl),
    		listInfo: listObject.list === undefined || listObject.list === null ? {} : listObject.list,
    		sharepointIds: listObject.sharepointIds === undefined || listObject.sharepointIds === null ? {} : listObject.sharepointIds
    	};
    };
    
    var gs_normalizeItem = function(itemObject, includeRaw) {
    	var mapped = {
    		itemId: gs_safeString(itemObject.id),
    		eTag: gs_safeString(itemObject.eTag),
    		createdDateTime: gs_safeString(itemObject.createdDateTime),
    		lastModifiedDateTime: gs_safeString(itemObject.lastModifiedDateTime),
    		webUrl: gs_safeString(itemObject.webUrl),
    		contentType: itemObject.contentType === undefined || itemObject.contentType === null ? {} : itemObject.contentType,
    		fields: itemObject.fields === undefined || itemObject.fields === null ? {} : itemObject.fields,
    		sharepointIds: itemObject.sharepointIds === undefined || itemObject.sharepointIds === null ? {} : itemObject.sharepointIds
    	};
    	if (includeRaw) {
    		mapped.raw = itemObject;
    	}
    	return mapped;
    };
    
    var graphOperation = "update_list_item";
    var requiredPermission = "Sites.ReadWrite.All";
    var tokenMode = "delegated_or_application";
    response = null;
    var requestedProvider = sppr_normalizeProvider(provider, "graph");
    
    if (requestedProvider === "onprem") {
    	tokenMode = "onprem";
    	try {
    		sppr_ensureOnPremDispatch();
    		response = sppr_onPremUpdateListItem({
    			siteBaseUrl: siteBaseUrl,
    			siteHostname: siteHostname,
    			sitePath: sitePath,
    			onPremProtocol: onPremProtocol,
    			listId: listId,
    			listName: listName,
    			itemId: itemId,
    			fieldsJson: fieldsJson,
    			ifMatch: "*",
    			returnUpdatedItem: returnUpdatedItem,
    			selectFields: selectFields,
    			expandFieldValuesAsText: false,
    			includeRawItem: includeRawItem,
    			accessToken: accessToken,
    			onPremUsername: onPremUsername,
    			onPremPassword: onPremPassword,
    			cookieHeader: cookieHeader
    		});
    	} catch (onPremError) {
    		response = gs_responseError(graphOperation, tokenMode, requiredPermission, onPremError);
    	}
    } else {
    	try {
    		var tokenInfo = gs_resolveToken(accessToken, tenantId, clientId, clientSecret);
    		tokenMode = tokenInfo.mode;
    
    		var targetItemId = String(gs_require("itemId", itemId)).trim();
    		if (targetItemId.length > 1 && targetItemId.charAt(0) === "\"" && targetItemId.charAt(targetItemId.length - 1) === "\"") {
    			targetItemId = targetItemId.substring(1, targetItemId.length - 1);
    		}
    		if (targetItemId.length === 0) {
    			throw new java.lang.IllegalArgumentException("itemId cannot be empty");
    		}
    		var fieldsObject = gs_parseJsonObject("fieldsJson", fieldsJson);
    		var includeRaw = gs_toBoolean(includeRawItem, false);
    		var shouldReturnUpdatedItem = gs_toBoolean(returnUpdatedItem, true);
    		var selectedFields = gs_splitCsv(selectFields);
    
    		var resolved = gs_resolveSiteAndList(
    			tokenInfo.token,
    			siteId,
    			siteHostname,
    			sitePath,
    			listId,
    			listName
    		);
    
    		var patchFieldsEndpoint =
    			"https://graph.microsoft.com/v1.0/sites/" + gs_encodePathSegment(resolved.siteId) +
    			"/lists/" + gs_encodePathSegment(resolved.listId) +
    			"/items/" + gs_encodePathSegment(targetItemId) +
    			"/fields";
    
    		var updatedFields = gs_graphPatchJson(patchFieldsEndpoint, tokenInfo.token, fieldsObject);
    		var item = {
    			itemId: String(targetItemId),
    			fields: updatedFields
    		};
    
    		if (shouldReturnUpdatedItem) {
    			var getUrl =
    				"https://graph.microsoft.com/v1.0/sites/" + gs_encodePathSegment(resolved.siteId) +
    				"/lists/" + gs_encodePathSegment(resolved.listId) +
    				"/items/" + gs_encodePathSegment(targetItemId);
    			var expandValue = "fields";
    			if (selectedFields.length > 0) {
    				expandValue = "fields($select=" + selectedFields.join(",") + ")";
    			}
    			getUrl = gs_appendQueryParam(getUrl, "$expand", expandValue);
    			item = gs_normalizeItem(gs_graphGetJson(getUrl, tokenInfo.token), includeRaw);
    		} else if (includeRaw) {
    			item.raw = updatedFields;
    		}
    
    		response = gs_responseOk(graphOperation, tokenMode, requiredPermission, {
    			siteId: resolved.siteId,
    			siteHostname: resolved.siteHostname,
    			sitePath: resolved.sitePath,
    			listId: resolved.listId,
    			listName: resolved.listName,
    			listDisplayName: resolved.listDisplayName,
    			listWebUrl: resolved.listWebUrl,
    			sharepointIds: resolved.sharepointIds,
    			listInfo: resolved.listInfo,
    			query: {
    				itemId: targetItemId,
    				returnUpdatedItem: shouldReturnUpdatedItem,
    				selectFields: selectedFields,
    				includeRawItem: includeRaw
    			},
    			item: item
    		});
    	} catch (e) {
    		response = gs_responseError(graphOperation, tokenMode, requiredPermission, e);
    	}
    }'
↓response [steps.JsonToXmlStep-1772620911003]: 
  comment: Converts the JS response object into XML output.
  jsonObject: 
    - xmlizable: 
      - ↑classname: com.twinsoft.convertigo.beans.steps.SmartType
      - SmartType: 
        - ↑mode: JS
        - →→: response
  key: 
    - xmlizable: 
      - ↑classname: com.twinsoft.convertigo.beans.steps.SmartType
      - SmartType: 
        - ↑mode: PLAIN
        - →→: response
↓siteId [variables.RequestableVariable-1772620911004]: 
  comment: Optional site identifier. If empty, siteHostname and sitePath are used to resolve it.
  description: Optional Graph site identifier
↓siteHostname [variables.RequestableVariable-1772620911005]: 
  comment: SharePoint hostname when resolving siteId, for example contoso.sharepoint.com.
  description: SharePoint hostname
  value: ${lib_Microsoft_Sharepoint.onPrem.http.server=sharepoint.local}
↓sitePath [variables.RequestableVariable-1772620911006]: 
  comment: Site path when resolving siteId, for example /sites/engineering.
  description: SharePoint site path
  value: ${lib_Microsoft_Sharepoint.onPrem.sitePath=/sites/test}
↓listId [variables.RequestableVariable-1772620911007]: 
  comment: Optional list identifier. If provided, listName is ignored.
  description: Optional Graph list identifier
↓listName [variables.RequestableVariable-1772620911008]: 
  comment: List display name or internal name used to resolve listId.
  description: SharePoint list name
  value: ${lib_Microsoft_Sharepoint.onPrem.listName=TestList}
↓itemId [variables.RequestableVariable-1772620911009]: 
  comment: Target SharePoint list item identifier.
  description: SharePoint list item id
  required: true
↓fieldsJson [variables.RequestableVariable-1772620911010]: 
  comment: JSON object payload used to patch list item fields.
  description: JSON object for fields patch payload
  required: true
  value: '{}'
↓returnUpdatedItem [variables.RequestableVariable-1772620911011]: 
  comment: true reloads updated item with optional field projection before returning.
  description: Return updated item details
  value: true
↓selectFields [variables.RequestableVariable-1772620911012]: 
  comment: Optional comma-separated list of field internal names projected in returned item.fields.
  description: Comma-separated field selection
↓includeRawItem [variables.RequestableVariable-1772620911013]: 
  comment: true includes raw Graph payload under item.raw.
  description: Include raw Graph payload
  value: false
↓accessToken [variables.RequestableVariable-1772620911014]: 
  comment: Optional delegated bearer token. If provided, tenant/client/secret are ignored.
  description: Delegated Microsoft Graph access token
↓tenantId [variables.RequestableVariable-1772620911015]: 
  comment: Azure Entra tenant id used for app-only token acquisition.
  description: Tenant id for app-only token
  value: ${Microsoft_AzGraph.tenantId}
↓clientId [variables.RequestableVariable-1772620911016]: 
  comment: Azure Entra application client id used for app-only token acquisition.
  description: Client id for app-only token
  value: ${Microsoft_AzGraph.clientId}
↓clientSecret [variables.RequestableVariable-1772620911017]: 
  comment: Azure Entra application client secret used for app-only token acquisition.
  description: Client secret for app-only token
  value: 
    - ↑ciphered: true
    - ↑traceable: false
    - →→: xa51e43ef1d108aa61557c46e9670abb3d50668c28091a8d24f75c1d2eb802780bc1e972ff7382dfe8bbbe42f37b74544
  visibility: '-1'
↓provider [variables.RequestableVariable-1772714469147]: 
  comment: Backend provider selection graph or onprem.
  description: Provider selection graph or onprem
  value: ${lib_Microsoft_Sharepoint.provider}
↓siteBaseUrl [variables.RequestableVariable-1772714469188]: 
  comment: Optional SharePoint on-prem site base URL, for example https://sharepoint.local/sites/intranet.
  description: On-prem site base URL
  value: ${lib_Microsoft_Sharepoint.onPrem.siteBaseUrl}
↓onPremProtocol [variables.RequestableVariable-1772714469228]: 
  comment: URL scheme fallback for on-prem mode when siteBaseUrl is not provided.
  description: On-prem protocol fallback
  value: ${lib_Microsoft_Sharepoint.onPrem.protocol=https}
↓onPremUsername [variables.RequestableVariable-1772714469278]: 
  comment: Optional technical username for on-prem basic authentication.
  description: On-prem technical username
  value: ${lib_Microsoft_Sharepoint.onPrem.username}
↓onPremPassword [variables.RequestableVariable-1772714469335]: 
  comment: Optional technical password for on-prem basic authentication.
  description: On-prem technical password
  value: 
    - ↑ciphered: true
    - ↑traceable: false
    - →→: x047adaa9fc7ed27aec6bc11e4a6b6d7cc0466d133c1a11849961023abeb405245a4fafc73fbab6b6999c294cb356940c0e6fa2827adf0d777f2f4f3e526ac991
  visibility: '-1'
↓cookieHeader [variables.RequestableVariable-1772714469408]: 
  comment: Optional Cookie header for on-prem forms authentication.
  description: Optional on-prem Cookie header
  value: 
    - ↑ciphered: true
    - ↑traceable: false
    - →→: x047adaa9fc7ed27aec6bc11e4a6b6d7cc0466d133c1a11849961023abeb40524d13591840f063c4918706e203c3286f7945be3bd1a0c33cb44b7f24f9d492d4c
  visibility: '-1'
↓TC_UpdateListItem_Graph [core.TestCase]: 
  ↓siteHostname [variables.TestCaseVariable-1772804004298]: 
    value: convertigo.sharepoint.com
  ↓sitePath [variables.TestCaseVariable-1772804004322]: 
    value: /sites/test1
  ↓listName [variables.TestCaseVariable-1772804004356]: 
    value: Web Template Extensions
  ↓itemId [variables.TestCaseVariable-1772804004397]: 
    value: '"8"'
  ↓fieldsJson [variables.TestCaseVariable-1772804004455]: 
  ↓provider [variables.TestCaseVariable-1772804004511]: 
    value: graph
↓TC_UpdateListItem_OnPrem [core.TestCase]: 
  ↓siteHostname [variables.TestCaseVariable-1772804004907]: 
    value: ${lib_Microsoft_Sharepoint.onPrem.http.server=sharepoint.local}
  ↓sitePath [variables.TestCaseVariable-1772804004935]: 
    value: ${lib_Microsoft_Sharepoint.onPrem.sitePath=/sites/test}
  ↓listName [variables.TestCaseVariable-1772804004971]: 
    value: ${lib_Microsoft_Sharepoint.onPrem.listName=TestList}
  ↓itemId [variables.TestCaseVariable-1772804005015]: 
    value: '"8"'
  ↓fieldsJson [variables.TestCaseVariable-1772804005075]: 
  ↓provider [variables.TestCaseVariable-1772804005141]: 
    value: onprem