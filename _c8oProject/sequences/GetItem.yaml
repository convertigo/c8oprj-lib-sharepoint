accessibility: Hidden
authenticatedContextRequired: true
comment: Retrieves one SharePoint drive item through Microsoft Graph (online) or SharePoint REST (on-prem). (Graph permissions Files.Read.All|Files.ReadWrite.All|Sites.Read.All|Sites.ReadWrite.All)
↓inputVars [steps.InputVariablesStep-1772719763456]: 
  comment: Exposes request inputs in XML for debugging and source mapping.
↓ExecuteGetDriveItem [steps.SimpleStep-1772719763479]: 
  comment: Resolves site and drive then retrieves one drive item through Microsoft Graph (online) or SharePoint REST (on-prem).
  expression: |
    'include("js/sharepoint_provider.js");
    include("js/sharepoint_graph_common.js");
    
    var graphOperation = "get_drive_item";
    var requiredPermission = "Files.Read.All|Files.ReadWrite.All|Sites.Read.All|Sites.ReadWrite.All";
    var tokenMode = "delegated_or_application";
    response = null;
    var requestedProvider = sppr_normalizeProvider(provider, "graph");
    
    if (requestedProvider === "onprem") {
    	tokenMode = "onprem";
    	try {
    		sppr_ensureOnPremDispatch();
    
    		var onPremCallSequence = function(sequenceName, parametersJS) {
    			var HashMap = java.util.HashMap;
    			var InternalRequester = Packages.com.twinsoft.convertigo.engine.requesters.InternalRequester;
    			var parameters = new HashMap();
    			var projectArray = java.lang.reflect.Array.newInstance(java.lang.String, 1);
    			projectArray[0] = spop_currentProjectName();
    			parameters.put("__project", projectArray);
    			parameters.put("__sequence", sequenceName);
    			parameters.put("__context", "syncContext_" + java.lang.System.currentTimeMillis());
    			var keys = Object.keys(parametersJS);
    			var i;
    			for (i = 0; i < keys.length; i++) {
    				var key = keys[i];
    				if (parametersJS[key] !== null && parametersJS[key] !== undefined) {
    					parameters.put(key, parametersJS[key]);
    				}
    			}
    			var requester = new InternalRequester(parameters, context.httpServletRequest);
    			try {
    				var innerResponse = requester.processRequest();
    				var converted = JSON.parse(String(Packages.com.twinsoft.convertigo.engine.util.XMLUtils.XmlToJson(innerResponse.getDocumentElement(), true, true, Packages.com.twinsoft.convertigo.engine.enums.JsonOutput.JsonRoot.docNode)));
    				org.apache.log4j.MDC.put("ContextualParameters", context.logParameters);
    				return gs_safeObject(gs_safeObject(converted.document).response);
    			} finally {
    				try {
    					var innerContext = requester.getContext();
    					theApp.contextManager.remove(innerContext);
    				} catch (ignoreInnerContext) {
    				}
    			}
    		};
    
    		var onPremMergeFields = function(requiredFields, optionalFields) {
    			var merged = [];
    			var seen = {};
    			var addFields = function(fields) {
    				var index;
    				for (index = 0; index < fields.length; index++) {
    					var name = gs_safeString(fields[index]);
    					if (!gs_isBlank(name) && seen[name] !== true) {
    						seen[name] = true;
    						merged.push(name);
    					}
    				}
    			};
    			addFields(requiredFields);
    			addFields(optionalFields);
    			return merged;
    		};
    
    		var onPremComposeAbsoluteUrl = function(targetSiteBaseUrl, serverRelativeUrl) {
    			var normalizedRelativeUrl = gs_safeString(serverRelativeUrl);
    			if (gs_isBlank(normalizedRelativeUrl)) {
    				return "";
    			}
    			try {
    				var parsedUrl = new java.net.URL(String(targetSiteBaseUrl));
    				var baseAuthority = parsedUrl.getProtocol() + "://" + parsedUrl.getHost();
    				var basePort = parsedUrl.getPort();
    				if (basePort > 0 && basePort !== parsedUrl.getDefaultPort()) {
    					baseAuthority += ":" + basePort;
    				}
    				return baseAuthority + normalizedRelativeUrl;
    			} catch (ignoreComposeAbsoluteUrl) {
    				return normalizedRelativeUrl;
    			}
    		};
    
    		var onPremBuildRootItem = function(resolvedLibrary, targetSiteBaseUrl, includeRaw) {
    			var rootFolderServerRelativeUrl = gs_safeString(gs_safeObject(resolvedLibrary.listInfo).rootFolderServerRelativeUrl);
    			var item = {
    				id: "root",
    				itemId: "root",
    				name: gs_safeString(resolvedLibrary.driveName),
    				webUrl: onPremComposeAbsoluteUrl(targetSiteBaseUrl, rootFolderServerRelativeUrl),
    				size: 0,
    				eTag: "",
    				cTag: "",
    				createdDateTime: gs_safeString(resolvedLibrary.createdDateTime),
    				lastModifiedDateTime: gs_safeString(resolvedLibrary.lastModifiedDateTime),
    				file: {},
    				folder: {},
    				image: {},
    				shared: {},
    				parentReference: {
    					driveId: gs_safeString(resolvedLibrary.driveId),
    					siteId: gs_safeString(resolvedLibrary.siteId),
    					path: rootFolderServerRelativeUrl
    				},
    				createdBy: {},
    				lastModifiedBy: {},
    				fileSystemInfo: {
    					createdDateTime: gs_safeString(resolvedLibrary.createdDateTime),
    					lastModifiedDateTime: gs_safeString(resolvedLibrary.lastModifiedDateTime)
    				},
    				sharepointIds: {
    					listId: gs_safeString(resolvedLibrary.listId),
    					siteId: gs_safeString(resolvedLibrary.siteId)
    				},
    				removed: {}
    			};
    			if (includeRaw) {
    				item.raw = {
    					rootFolderServerRelativeUrl: rootFolderServerRelativeUrl,
    					library: resolvedLibrary
    				};
    			}
    			return item;
    		};
    
    		var onPremBuildDriveItemFromListItem = function(resolvedLibrary, listItemResponse, targetSiteBaseUrl, includeRaw) {
    			var itemObject = gs_safeObject(gs_safeObject(listItemResponse.data).item);
    			var itemFields = gs_safeObject(itemObject.fields);
    			var fileSystemType = gs_safeString(itemFields.FileSystemObjectType);
    			if (gs_isBlank(fileSystemType)) {
    				fileSystemType = gs_safeString(itemFields.FSObjType);
    			}
    			var isFolder = fileSystemType === "1";
    			var itemServerRelativeUrl = gs_safeString(itemFields.FileRef);
    			var parentServerRelativeUrl = gs_safeString(itemFields.FileDirRef);
    			var itemName = gs_safeString(itemFields.FileLeafRef);
    			if (gs_isBlank(itemName)) {
    				itemName = gs_safeString(itemObject.title);
    			}
    			var sizeValue = 0;
    			if (!isFolder && !gs_isBlank(itemFields.File_x0020_Size)) {
    				sizeValue = gs_parseInteger("size", itemFields.File_x0020_Size, 0);
    			}
    
    			var normalizedItem = {
    				id: gs_safeString(itemObject.itemId),
    				itemId: gs_safeString(itemObject.itemId),
    				name: itemName,
    				webUrl: onPremComposeAbsoluteUrl(targetSiteBaseUrl, itemServerRelativeUrl),
    				size: sizeValue,
    				eTag: "",
    				cTag: "",
    				createdDateTime: gs_safeString(itemObject.created),
    				lastModifiedDateTime: gs_safeString(itemObject.modified),
    				file: isFolder ? {} : {},
    				folder: isFolder ? { childCount: 0 } : {},
    				image: {},
    				shared: {},
    				parentReference: {
    					driveId: gs_safeString(resolvedLibrary.driveId),
    					siteId: gs_safeString(resolvedLibrary.siteId),
    					path: parentServerRelativeUrl
    				},
    				createdBy: {
    					userId: gs_safeString(itemFields.AuthorId)
    				},
    				lastModifiedBy: {
    					userId: gs_safeString(itemFields.EditorId)
    				},
    				fileSystemInfo: {
    					createdDateTime: gs_safeString(itemObject.created),
    					lastModifiedDateTime: gs_safeString(itemObject.modified)
    				},
    				sharepointIds: {
    					listId: gs_safeString(resolvedLibrary.listId),
    					siteId: gs_safeString(resolvedLibrary.siteId),
    					listItemId: gs_safeString(itemObject.itemId),
    					uniqueId: gs_safeString(itemFields.UniqueId)
    				},
    				removed: {}
    			};
    			if (includeRaw) {
    				normalizedItem.raw = itemObject.raw === null || itemObject.raw === undefined ? itemFields : itemObject.raw;
    			}
    			return normalizedItem;
    		};
    
    		var includeRaw = gs_toBoolean(includeRawItem, false);
    		var selectedFields = gs_splitCsv(selectFields);
    		var requestedItemId = gs_safeString(itemId);
    		var normalizedItemId = gs_isBlank(requestedItemId) ? "root" : requestedItemId;
    		var targetSiteBaseUrl = spop_buildSiteBaseUrl(siteBaseUrl, siteHostname, sitePath, onPremProtocol);
    
    		var resolvedLibraryResponse = onPremCallSequence("ResolveLibrary", {
    			provider: "onprem",
    			siteBaseUrl: siteBaseUrl,
    			siteHostname: siteHostname,
    			sitePath: sitePath,
    			onPremProtocol: onPremProtocol,
    			driveId: driveId,
    			driveName: driveName,
    			listId: listId,
    			listName: listName,
    			accessToken: accessToken,
    			onPremUsername: onPremUsername,
    			onPremPassword: onPremPassword,
    			cookieHeader: cookieHeader
    		});
    		if (resolvedLibraryResponse === null || resolvedLibraryResponse === undefined) {
    			throw new java.lang.RuntimeException("resolve_drive returned no response");
    		}
    		if (resolvedLibraryResponse.ok !== true) {
    			throw new java.lang.RuntimeException("resolve_drive failed: " + gs_safeString(resolvedLibraryResponse.errorMessage));
    		}
    		if (!gs_isBlank(resolvedLibraryResponse.tokenMode)) {
    			tokenMode = gs_safeString(resolvedLibraryResponse.tokenMode);
    		}
    
    		var resolvedLibrary = gs_safeObject(resolvedLibraryResponse.data);
    		var item = null;
    		if (normalizedItemId === "root") {
    			item = onPremBuildRootItem(resolvedLibrary, targetSiteBaseUrl, includeRaw);
    		} else {
    			var listItemResponse = sppr_onPremGetListItem({
    				siteBaseUrl: targetSiteBaseUrl,
    				siteHostname: siteHostname,
    				sitePath: sitePath,
    				onPremProtocol: onPremProtocol,
    				listId: gs_safeString(resolvedLibrary.listId),
    				listName: gs_safeString(resolvedLibrary.listName),
    				itemId: normalizedItemId,
    				selectFields: "",
    				includeRawItem: includeRaw ? "true" : "false",
    				accessToken: accessToken,
    				onPremUsername: onPremUsername,
    				onPremPassword: onPremPassword,
    				cookieHeader: cookieHeader
    			});
    			if (listItemResponse === null || listItemResponse === undefined) {
    				throw new java.lang.RuntimeException("onprem_get_list_item returned no response");
    			}
    			if (listItemResponse.ok !== true) {
    				throw new java.lang.RuntimeException("onprem_get_list_item failed: " + gs_safeString(listItemResponse.errorMessage));
    			}
    			item = onPremBuildDriveItemFromListItem(resolvedLibrary, listItemResponse, targetSiteBaseUrl, includeRaw);
    		}
    
    		response = gs_responseOk(graphOperation, tokenMode, requiredPermission, {
    			siteId: gs_safeString(resolvedLibrary.siteId),
    			siteHostname: gs_safeString(resolvedLibrary.siteHostname),
    			sitePath: gs_safeString(resolvedLibrary.sitePath),
    			driveId: gs_safeString(resolvedLibrary.driveId),
    			driveName: gs_safeString(resolvedLibrary.driveName),
    			driveWebUrl: gs_safeString(resolvedLibrary.driveWebUrl),
    			driveType: gs_safeString(resolvedLibrary.driveType),
    			sharepointIds: gs_safeObject(resolvedLibrary.sharepointIds),
    			quota: gs_safeObject(resolvedLibrary.quota),
    			owner: gs_safeObject(resolvedLibrary.owner),
    			resolvedFromList: true,
    			listId: gs_safeString(resolvedLibrary.listId),
    			listName: gs_safeString(resolvedLibrary.listName),
    			listDisplayName: gs_safeString(resolvedLibrary.listDisplayName),
    			listWebUrl: gs_safeString(resolvedLibrary.listWebUrl),
    			listInfo: gs_safeObject(resolvedLibrary.listInfo),
    			listSharepointIds: gs_safeObject(resolvedLibrary.listSharepointIds),
    			query: {
    				itemId: normalizedItemId,
    				selectFields: selectedFields,
    				includeRawItem: includeRaw
    			},
    			item: item
    		});
    	} catch (onPremError) {
    		response = gs_responseError(graphOperation, tokenMode, requiredPermission, onPremError);
    	}
    } else {
    	try {
    	var tokenInfo = gs_resolveToken(accessToken, tenantId, clientId, clientSecret);
    	tokenMode = tokenInfo.mode;
    
    	var includeRaw = gs_toBoolean(includeRawItem, false);
    	var selectedFields = gs_splitCsv(selectFields);
    	var requestedItemId = gs_safeString(itemId);
    	var normalizedItemId = gs_isBlank(requestedItemId) ? "root" : requestedItemId;
    
    	var resolvedSite = gs_resolveSite(tokenInfo.token, siteId, siteHostname, sitePath);
    	var resolvedDriveData = gs_resolveDrive(tokenInfo.token, resolvedSite, driveId, driveName, listId, listName);
    	var resolvedDrive = resolvedDriveData.drive;
    	var resolvedList = resolvedDriveData.list;
    
    	var requestInfo = gs_graphDriveItemGetRequestInfo(tokenInfo.token, resolvedDrive.driveId, requestedItemId);
    	if (selectedFields.length > 0) {
    		gs_graphAddQueryParameter(requestInfo, "$select", selectedFields.join(","));
    	}
    
    	var item = gs_normalizeDriveItem(gs_graphGetJsonRequestInfo(requestInfo, tokenInfo.token), includeRaw);
    
    	response = gs_responseOk(graphOperation, tokenMode, requiredPermission, {
    		siteId: resolvedSite.siteId,
    		siteHostname: resolvedSite.siteHostname,
    		sitePath: resolvedSite.sitePath,
    		driveId: resolvedDrive.driveId,
    		driveName: resolvedDrive.driveName,
    		driveWebUrl: resolvedDrive.driveWebUrl,
    		driveType: resolvedDrive.driveType,
    		sharepointIds: resolvedDrive.sharepointIds,
    		quota: resolvedDrive.quota,
    		owner: resolvedDrive.owner,
    		resolvedFromList: resolvedList !== null,
    		listId: resolvedList === null ? "" : resolvedList.listId,
    		listName: resolvedList === null ? "" : resolvedList.listName,
    		listDisplayName: resolvedList === null ? "" : resolvedList.listDisplayName,
    		listWebUrl: resolvedList === null ? "" : resolvedList.listWebUrl,
    		listInfo: resolvedList === null ? {} : resolvedList.listInfo,
    		listSharepointIds: resolvedList === null ? {} : resolvedList.sharepointIds,
    		query: {
    			itemId: normalizedItemId,
    			selectFields: selectedFields,
    			includeRawItem: includeRaw
    		},
    		item: item
    	});
    	} catch (e) {
    		response = gs_responseError(graphOperation, tokenMode, requiredPermission, e);
    	}
    }'
↓response [steps.JsonToXmlStep-1772719763512]: 
  comment: Converts the JS response object into XML output.
  jsonObject: 
    - xmlizable: 
      - ↑classname: com.twinsoft.convertigo.beans.steps.SmartType
      - SmartType: 
        - ↑mode: JS
        - →→: response
  key: 
    - xmlizable: 
      - ↑classname: com.twinsoft.convertigo.beans.steps.SmartType
      - SmartType: 
        - ↑mode: PLAIN
        - →→: response
↓siteId [variables.RequestableVariable-1772719763550]: 
  comment: Optional site identifier. If empty, siteHostname and sitePath are used to resolve it.
  description: Optional Graph site identifier
↓siteHostname [variables.RequestableVariable-1772719763595]: 
  comment: SharePoint Online hostname when resolving siteId, for example contoso.sharepoint.com.
  description: SharePoint Online hostname
  value: ${lib_Microsoft_Sharepoint.onPrem.http.server=sharepoint.local}
↓sitePath [variables.RequestableVariable-1772719763649]: 
  comment: Site path when resolving siteId, for example /sites/engineering.
  description: SharePoint site path
  value: ${lib_Microsoft_Sharepoint.onPrem.sitePath=/sites/test}
↓driveId [variables.RequestableVariable-1772719763711]: 
  comment: Optional drive identifier. If provided, driveName/listId/listName are ignored.
  description: Optional Graph drive identifier
↓driveName [variables.RequestableVariable-1772719763782]: 
  comment: Drive name used to resolve driveId when driveId is not provided.
  description: SharePoint drive name
  value: ${lib_Microsoft_Sharepoint.onPrem.driveName=TestLibrary}
↓listId [variables.RequestableVariable-1772719763862]: 
  comment: Optional list identifier used to resolve associated drive.
  description: Optional Graph list identifier
↓listName [variables.RequestableVariable-1772719763950]: 
  comment: List display name or internal name used to resolve associated drive.
  description: SharePoint list name
  value: ${lib_Microsoft_Sharepoint.onPrem.listName=TestList}
↓itemId [variables.RequestableVariable-1772719764044]: 
  comment: Target drive item identifier. Use root or leave empty to retrieve drive root metadata.
  description: Target drive item id
  value: root
↓selectFields [variables.RequestableVariable-1772719764146]: 
  comment: Optional comma-separated list of Graph driveItem properties to select.
  description: Comma-separated property selection
↓includeRawItem [variables.RequestableVariable-1772719764249]: 
  comment: true includes raw Graph driveItem payload under response.data.item.raw.
  description: Include raw Graph payload
  value: false
↓provider [variables.RequestableVariable-1773062134294]: 
  comment: Backend provider selection graph or onprem.
  description: Provider selection graph or onprem
  value: ${lib_Microsoft_Sharepoint.provider}
↓siteBaseUrl [variables.RequestableVariable-1773065887223]: 
  comment: Optional SharePoint on-prem site base URL, for example https://sharepoint.local/sites/intranet.
  description: On-prem site base URL
  value: ${lib_Microsoft_Sharepoint.onPrem.siteBaseUrl}
↓onPremProtocol [variables.RequestableVariable-1773065887334]: 
  comment: URL scheme fallback for on-prem mode when siteBaseUrl is not provided.
  description: On-prem protocol fallback
  value: ${lib_Microsoft_Sharepoint.onPrem.protocol}
↓onPremUsername [variables.RequestableVariable-1773065887383]: 
  comment: Optional technical username for on-prem basic authentication.
  description: On-prem technical username
  value: ${lib_Microsoft_Sharepoint.onPrem.username}
↓onPremPassword [variables.RequestableVariable-1773065887438]: 
  comment: Optional technical password for on-prem basic authentication.
  description: On-prem technical password
  value: 
    - ↑ciphered: true
    - ↑traceable: false
    - →→: x047adaa9fc7ed27aec6bc11e4a6b6d7cc0466d133c1a11849961023abeb405245a4fafc73fbab6b6999c294cb356940c0e6fa2827adf0d777f2f4f3e526ac991
  visibility: '-1'
↓cookieHeader [variables.RequestableVariable-1773065887496]: 
  comment: Optional Cookie header for on-prem forms authentication.
  description: Optional on-prem Cookie header
  value: 
    - ↑ciphered: true
    - ↑traceable: false
    - →→: xd8e180806a1c1d83a6255212121ab827
  visibility: '-1'
↓accessToken [variables.RequestableVariable-1772719764364]: 
  comment: Optional delegated bearer token. If provided, tenant/client/secret are ignored.
  description: Delegated Microsoft Graph access token
↓tenantId [variables.RequestableVariable-1772719764489]: 
  comment: Azure Entra tenant id used for app-only token acquisition.
  description: Tenant id for app-only token
  value: ${Microsoft_AzGraph.tenantId}
↓clientId [variables.RequestableVariable-1772719764620]: 
  comment: Azure Entra application client id used for app-only token acquisition.
  description: Client id for app-only token
  value: ${Microsoft_AzGraph.clientId}
↓clientSecret [variables.RequestableVariable-1772719764763]: 
  comment: Azure Entra application client secret used for app-only token acquisition.
  description: Client secret for app-only token
  value: 
    - ↑ciphered: true
    - ↑traceable: false
    - →→: xa51e43ef1d108aa61557c46e9670abb3d50668c28091a8d24f75c1d2eb802780bc1e972ff7382dfe8bbbe42f37b74544
  visibility: '-1'
↓TC_GetDriveItem [core.TestCase]: 
  ↓siteHostname [variables.TestCaseVariable-1772719766105]: 
    value: convertigo.sharepoint.com
  ↓sitePath [variables.TestCaseVariable-1772719766129]: 
    value: /sites/test1
  ↓driveName [variables.TestCaseVariable-1772719766160]: 
    value: Documents
  ↓itemId [variables.TestCaseVariable-1772719766199]: 
    value: root
↓TC_GetDriveItem_OnPrem [core.TestCase]: 
  ↓siteHostname [variables.TestCaseVariable-1773065889150]: 
    value: ${lib_Microsoft_Sharepoint.onPrem.http.server=sharepoint.local}
  ↓sitePath [variables.TestCaseVariable-1773065889178]: 
    value: ${lib_Microsoft_Sharepoint.onPrem.sitePath=/sites/test}
  ↓driveName [variables.TestCaseVariable-1773065889217]: 
    value: ${lib_Microsoft_Sharepoint.onPrem.driveName=TestLibrary}
  ↓itemId [variables.TestCaseVariable-1773065889259]: 
    value: root
  ↓provider [variables.TestCaseVariable-1773065889314]: 
    value: onprem