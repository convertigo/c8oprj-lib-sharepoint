accessibility: Hidden
authenticatedContextRequired: true
comment: Creates one SharePoint drive item sharing link through Microsoft Graph (online), with graceful fallback when unsupported on-prem. (Graph permissions Files.ReadWrite.All|Sites.ReadWrite.All)
↓inputVars [steps.InputVariablesStep-1772719747678]: 
  comment: Exposes request inputs in XML for debugging and source mapping.
↓ExecuteCreateDriveItemShareLink [steps.SimpleStep-1772719747727]: 
  comment: Resolves site and drive then creates one sharing link for a drive item.
  expression: |
    'include("js/sharepoint_provider.js");
    include("js/sharepoint_graph_common.js");
    
    var graphOperation = "create_drive_item_share_link";
    var requiredPermission = "Files.ReadWrite.All|Sites.ReadWrite.All";
    var tokenMode = "delegated_or_application";
    response = null;
    var requestedProvider = sppr_normalizeProvider(provider, "graph");
    
    if (requestedProvider === "onprem") {
    	tokenMode = "onprem";
    	response = gs_responseOk(graphOperation, tokenMode, requiredPermission, {
    		supported: false,
    		fallbackStatus: "onprem_not_supported",
    		message: "Create share link is not supported for SharePoint on-prem in unified mode. Use provider=graph.",
    		query: {
    			itemId: gs_safeString(itemId),
    			linkType: gs_defaultString(linkType, "view").toLowerCase(),
    			scope: gs_defaultString(scope, "organization").toLowerCase(),
    			passwordProvided: !gs_isBlank(password),
    			expirationDateTimeProvided: !gs_isBlank(expirationDateTime),
    			retainInheritedPermissions: gs_toBoolean(retainInheritedPermissions, true),
    			includeRawPermission: gs_toBoolean(includeRawPermission, false)
    		},
    		permission: {}
    	});
    } else {
    	try {
    		var tokenInfo = gs_resolveToken(accessToken, tenantId, clientId, clientSecret);
    		tokenMode = tokenInfo.mode;
    
    		var targetItemId = gs_require("itemId", itemId);
    		if (String(targetItemId).toLowerCase() === "root") {
    			throw new java.lang.IllegalArgumentException("Creating share link on drive root is not allowed. Provide a child item id.");
    		}
    
    		var requestedLinkType = gs_defaultString(linkType, "view").toLowerCase();
    		var requestedScope = gs_defaultString(scope, "organization").toLowerCase();
    		var requestedPassword = gs_safeString(password);
    		var requestedExpiration = gs_safeString(expirationDateTime);
    		var requestedRetainInherited = gs_toBoolean(retainInheritedPermissions, true);
    		var includeRaw = gs_toBoolean(includeRawPermission, false);
    
    		if (requestedLinkType !== "view" && requestedLinkType !== "edit" && requestedLinkType !== "embed" && requestedLinkType !== "blocksDownload") {
    			throw new java.lang.IllegalArgumentException("linkType must be one of: view, edit, embed, blocksDownload");
    		}
    		if (requestedScope !== "anonymous" && requestedScope !== "organization" && requestedScope !== "users") {
    			throw new java.lang.IllegalArgumentException("scope must be one of: anonymous, organization, users");
    		}
    
    		var resolvedSite = gs_resolveSite(tokenInfo.token, siteId, siteHostname, sitePath);
    		var resolvedDriveData = gs_resolveDrive(tokenInfo.token, resolvedSite, driveId, driveName, listId, listName);
    		var resolvedDrive = resolvedDriveData.drive;
    		var resolvedList = resolvedDriveData.list;
    
    		var createLinkObject = {
    			type: requestedLinkType,
    			scope: requestedScope,
    			retainInheritedPermissions: requestedRetainInherited
    		};
    		if (!gs_isBlank(requestedPassword)) {
    			createLinkObject.password = requestedPassword;
    		}
    		if (!gs_isBlank(requestedExpiration)) {
    			createLinkObject.expirationDateTime = requestedExpiration;
    		}
    
    		var createLinkRequestInfo = gs_graphDriveItemCreateLinkRequestInfo(tokenInfo.token, resolvedDrive.driveId, targetItemId);
    		var createLinkResponse = gs_graphPostRequestInfo(createLinkRequestInfo, tokenInfo.token, createLinkObject, null);
    		if (createLinkResponse.statusCode < 200 || createLinkResponse.statusCode >= 300) {
    			throw new java.lang.RuntimeException("Graph createLink failed (" + createLinkResponse.statusCode + "): " + createLinkResponse.body);
    		}
    		if (gs_isBlank(createLinkResponse.body)) {
    			throw new java.lang.RuntimeException("Graph createLink returned empty body");
    		}
    		var permissionObject = JSON.parse(createLinkResponse.body);
    		var responsePermission = gs_normalizePermission(permissionObject, includeRaw);
    
    		response = gs_responseOk(graphOperation, tokenMode, requiredPermission, {
    			siteId: resolvedSite.siteId,
    			siteHostname: resolvedSite.siteHostname,
    			sitePath: resolvedSite.sitePath,
    			driveId: resolvedDrive.driveId,
    			driveName: resolvedDrive.driveName,
    			driveWebUrl: resolvedDrive.driveWebUrl,
    			driveType: resolvedDrive.driveType,
    			sharepointIds: resolvedDrive.sharepointIds,
    			quota: resolvedDrive.quota,
    			owner: resolvedDrive.owner,
    			resolvedFromList: resolvedList !== null,
    			listId: resolvedList === null ? "" : resolvedList.listId,
    			listName: resolvedList === null ? "" : resolvedList.listName,
    			listDisplayName: resolvedList === null ? "" : resolvedList.listDisplayName,
    			listWebUrl: resolvedList === null ? "" : resolvedList.listWebUrl,
    			listInfo: resolvedList === null ? {} : resolvedList.listInfo,
    			listSharepointIds: resolvedList === null ? {} : resolvedList.sharepointIds,
    			query: {
    				itemId: targetItemId,
    				linkType: requestedLinkType,
    				scope: requestedScope,
    				passwordProvided: !gs_isBlank(requestedPassword),
    				expirationDateTimeProvided: !gs_isBlank(requestedExpiration),
    				retainInheritedPermissions: requestedRetainInherited,
    				includeRawPermission: includeRaw
    			},
    			permission: responsePermission
    		});
    	} catch (e) {
    		response = gs_responseError(graphOperation, tokenMode, requiredPermission, e);
    	}
    }'
↓response [steps.JsonToXmlStep-1772719747757]: 
  comment: Converts the JS response object into XML output.
  jsonObject: 
    - xmlizable: 
      - ↑classname: com.twinsoft.convertigo.beans.steps.SmartType
      - SmartType: 
        - ↑mode: JS
        - →→: response
  key: 
    - xmlizable: 
      - ↑classname: com.twinsoft.convertigo.beans.steps.SmartType
      - SmartType: 
        - ↑mode: PLAIN
        - →→: response
↓siteId [variables.RequestableVariable-1772719747793]: 
  comment: Optional site identifier. If empty, siteHostname and sitePath are used to resolve it.
  description: Optional Graph site identifier
↓siteHostname [variables.RequestableVariable-1772719747838]: 
  comment: SharePoint hostname when resolving siteId, for example contoso.sharepoint.com.
  description: SharePoint hostname
  value: ${lib_Microsoft_Sharepoint.onPrem.http.server=sharepoint.local}
↓sitePath [variables.RequestableVariable-1772719747893]: 
  comment: Site path when resolving siteId, for example /sites/engineering.
  description: SharePoint site path
  value: ${lib_Microsoft_Sharepoint.onPrem.sitePath=/sites/test}
↓driveId [variables.RequestableVariable-1772719747952]: 
  comment: Optional drive identifier. If provided, driveName/listId/listName are ignored.
  description: Optional Graph drive identifier
↓driveName [variables.RequestableVariable-1772719748018]: 
  comment: Drive name used to resolve driveId when driveId is not provided.
  description: SharePoint drive name
  value: ${lib_Microsoft_Sharepoint.onPrem.driveName=TestLibrary}
↓listId [variables.RequestableVariable-1772719748092]: 
  comment: Optional list identifier used to resolve associated drive.
  description: Optional Graph list identifier
↓listName [variables.RequestableVariable-1772719748173]: 
  comment: List display name or internal name used to resolve associated drive.
  description: SharePoint list name
  value: ${lib_Microsoft_Sharepoint.onPrem.listName=TestList}
↓itemId [variables.RequestableVariable-1772719748267]: 
  comment: Target drive item identifier.
  description: Target drive item id
↓linkType [variables.RequestableVariable-1772719748362]: 
  comment: Sharing link type (view, edit, embed, blocksDownload).
  description: Link type
  value: view
↓scope [variables.RequestableVariable-1772719748465]: 
  comment: Sharing link scope (anonymous, organization, users).
  description: Link scope
  value: organization
↓password [variables.RequestableVariable-1772719748575]: 
  comment: Optional password used when scope/type supports it.
  description: Optional link password
↓expirationDateTime [variables.RequestableVariable-1772719748694]: 
  comment: Optional link expiration datetime in ISO-8601 format.
  description: Optional expiration datetime
↓retainInheritedPermissions [variables.RequestableVariable-1772719748827]: 
  comment: Retains inherited permissions on first share when true.
  description: Retain inherited permissions
  value: true
↓includeRawPermission [variables.RequestableVariable-1772719748972]: 
  comment: true includes raw Graph permission payload under response.data.permission.raw.
  description: Include raw Graph permission payload
  value: false
↓provider [variables.RequestableVariable-1772719749115]: 
  comment: Backend provider selection graph or onprem.
  description: Provider selection graph or onprem
  value: ${lib_Microsoft_Sharepoint.provider}
↓accessToken [variables.RequestableVariable-1772719749261]: 
  comment: Optional delegated bearer token. If provided, tenant/client/secret are ignored.
  description: Delegated Microsoft Graph access token
↓tenantId [variables.RequestableVariable-1772719749414]: 
  comment: Azure Entra tenant id used for app-only token acquisition.
  description: Tenant id for app-only token
  value: ${Microsoft_AzGraph.tenantId}
↓clientId [variables.RequestableVariable-1772719749580]: 
  comment: Azure Entra application client id used for app-only token acquisition.
  description: Client id for app-only token
  value: ${Microsoft_AzGraph.clientId}
↓clientSecret [variables.RequestableVariable-1772719749759]: 
  comment: Azure Entra application client secret used for app-only token acquisition.
  description: Client secret for app-only token
  value: 
    - ↑ciphered: true
    - ↑traceable: false
    - →→: xa51e43ef1d108aa61557c46e9670abb3d50668c28091a8d24f75c1d2eb802780bc1e972ff7382dfe8bbbe42f37b74544
  visibility: '-1'
↓TC_CreateDriveItemShareLink_Graph [core.TestCase]: 
  ↓siteHostname [variables.TestCaseVariable-1772803979750]: 
    value: convertigo.sharepoint.com
  ↓sitePath [variables.TestCaseVariable-1772803979776]: 
    value: /sites/test1
  ↓driveName [variables.TestCaseVariable-1772803979809]: 
    value: Documents
  ↓itemId [variables.TestCaseVariable-1772803979856]: 
    value: 01PETTHVHBULYXIKX2EZFIAFNZVAYMGFUP
  ↓linkType [variables.TestCaseVariable-1772803979922]: 
    value: view
  ↓scope [variables.TestCaseVariable-1772803979980]: 
    value: organization
  ↓provider [variables.TestCaseVariable-1772803980045]: 
    value: graph
↓TC_CreateDriveItemShareLink_OnPrem [core.TestCase]: 
  ↓siteHostname [variables.TestCaseVariable-1772803980970]: 
    value: ${lib_Microsoft_Sharepoint.onPrem.http.server=sharepoint.local}
  ↓sitePath [variables.TestCaseVariable-1772803981002]: 
    value: ${lib_Microsoft_Sharepoint.onPrem.sitePath=/sites/test}
  ↓driveName [variables.TestCaseVariable-1772803981053]: 
    value: ${lib_Microsoft_Sharepoint.onPrem.driveName=TestLibrary}
  ↓itemId [variables.TestCaseVariable-1772803981113]: 
    value: 01PETTHVHBULYXIKX2EZFIAFNZVAYMGFUP
  ↓linkType [variables.TestCaseVariable-1772803981197]: 
    value: view
  ↓scope [variables.TestCaseVariable-1772803981273]: 
    value: organization
  ↓provider [variables.TestCaseVariable-1772803981353]: 
    value: onprem