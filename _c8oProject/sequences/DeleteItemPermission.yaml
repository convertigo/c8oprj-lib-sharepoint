accessibility: Hidden
authenticatedContextRequired: true
comment: Deletes one SharePoint drive item permission through Microsoft Graph (online), with graceful fallback when unsupported on-prem. (Graph permissions Files.ReadWrite.All|Sites.ReadWrite.All)
↓inputVars [steps.InputVariablesStep-1772719755104]: 
  comment: Exposes request inputs in XML for debugging and source mapping.
↓ExecuteDeleteDriveItemPermission [steps.SimpleStep-1772719755124]: 
  comment: Resolves site and drive then deletes one permission on a drive item.
  expression: |
    'include("js/sharepoint_provider.js");
    include("js/sharepoint_graph_common.js");
    
    var graphOperation = "delete_drive_item_permission";
    var requiredPermission = "Files.ReadWrite.All|Sites.ReadWrite.All";
    var tokenMode = "delegated_or_application";
    response = null;
    var requestedProvider = sppr_normalizeProvider(provider, "graph");
    
    if (requestedProvider === "onprem") {
    	tokenMode = "onprem";
    	response = gs_responseOk(graphOperation, tokenMode, requiredPermission, {
    		supported: false,
    		fallbackStatus: "onprem_not_supported",
    		message: "Deleting drive item permission is not supported for SharePoint on-prem in unified mode. Use provider=graph.",
    		query: {
    			itemId: gs_safeString(itemId),
    			permissionId: gs_safeString(permissionId),
    			includeDeletedPermissionSnapshot: gs_toBoolean(includeDeletedPermissionSnapshot, false),
    			includeRawPermission: gs_toBoolean(includeRawPermission, false)
    		},
    		deleted: false,
    		deleteHttpStatus: 501,
    		permission: {
    			permissionId: gs_safeString(permissionId)
    		}
    	});
    } else {
    	try {
    		var tokenInfo = gs_resolveToken(accessToken, tenantId, clientId, clientSecret);
    		tokenMode = tokenInfo.mode;
    
    		var targetItemId = gs_require("itemId", itemId);
    		if (String(targetItemId).toLowerCase() === "root") {
    			throw new java.lang.IllegalArgumentException("Deleting permission on drive root is not allowed. Provide a child item id.");
    		}
    		var targetPermissionId = gs_require("permissionId", permissionId);
    
    		var includeSnapshot = gs_toBoolean(includeDeletedPermissionSnapshot, false);
    		var includeRaw = gs_toBoolean(includeRawPermission, false);
    
    		var resolvedSite = gs_resolveSite(tokenInfo.token, siteId, siteHostname, sitePath);
    		var resolvedDriveData = gs_resolveDrive(tokenInfo.token, resolvedSite, driveId, driveName, listId, listName);
    		var resolvedDrive = resolvedDriveData.drive;
    		var resolvedList = resolvedDriveData.list;
    
    		var snapshot = {};
    		if (includeSnapshot) {
    			var snapshotRequestInfo = gs_graphDriveItemPermissionGetRequestInfo(tokenInfo.token, resolvedDrive.driveId, targetItemId, targetPermissionId);
    			snapshot = gs_normalizePermission(gs_graphGetJsonRequestInfo(snapshotRequestInfo, tokenInfo.token), includeRaw);
    		}
    
    		var deleteRequestInfo = gs_graphDriveItemPermissionDeleteRequestInfo(tokenInfo.token, resolvedDrive.driveId, targetItemId, targetPermissionId);
    		var deleteResult = gs_graphDeleteRequestInfo(deleteRequestInfo, tokenInfo.token);
    
    		response = gs_responseOk(graphOperation, tokenMode, requiredPermission, {
    			siteId: resolvedSite.siteId,
    			siteHostname: resolvedSite.siteHostname,
    			sitePath: resolvedSite.sitePath,
    			driveId: resolvedDrive.driveId,
    			driveName: resolvedDrive.driveName,
    			driveWebUrl: resolvedDrive.driveWebUrl,
    			driveType: resolvedDrive.driveType,
    			sharepointIds: resolvedDrive.sharepointIds,
    			quota: resolvedDrive.quota,
    			owner: resolvedDrive.owner,
    			resolvedFromList: resolvedList !== null,
    			listId: resolvedList === null ? "" : resolvedList.listId,
    			listName: resolvedList === null ? "" : resolvedList.listName,
    			listDisplayName: resolvedList === null ? "" : resolvedList.listDisplayName,
    			listWebUrl: resolvedList === null ? "" : resolvedList.listWebUrl,
    			listInfo: resolvedList === null ? {} : resolvedList.listInfo,
    			listSharepointIds: resolvedList === null ? {} : resolvedList.sharepointIds,
    			query: {
    				itemId: targetItemId,
    				permissionId: targetPermissionId,
    				includeDeletedPermissionSnapshot: includeSnapshot,
    				includeRawPermission: includeRaw
    			},
    			deleted: deleteResult.statusCode === 204 || deleteResult.statusCode === 404,
    			deleteHttpStatus: deleteResult.statusCode,
    			permission: includeSnapshot ? snapshot : {
    				permissionId: String(targetPermissionId)
    			}
    		});
    	} catch (e) {
    		response = gs_responseError(graphOperation, tokenMode, requiredPermission, e);
    	}
    }'
↓response [steps.JsonToXmlStep-1772719755159]: 
  comment: Converts the JS response object into XML output.
  jsonObject: 
    - xmlizable: 
      - ↑classname: com.twinsoft.convertigo.beans.steps.SmartType
      - SmartType: 
        - ↑mode: JS
        - →→: response
  key: 
    - xmlizable: 
      - ↑classname: com.twinsoft.convertigo.beans.steps.SmartType
      - SmartType: 
        - ↑mode: PLAIN
        - →→: response
↓siteId [variables.RequestableVariable-1772719755196]: 
  comment: Optional site identifier. If empty, siteHostname and sitePath are used to resolve it.
  description: Optional Graph site identifier
↓siteHostname [variables.RequestableVariable-1772719755239]: 
  comment: SharePoint hostname when resolving siteId, for example contoso.sharepoint.com.
  description: SharePoint hostname
  value: ${lib_Microsoft_Sharepoint.onPrem.http.server=sharepoint.local}
↓sitePath [variables.RequestableVariable-1772719755290]: 
  comment: Site path when resolving siteId, for example /sites/engineering.
  description: SharePoint site path
  value: ${lib_Microsoft_Sharepoint.onPrem.sitePath=/sites/test}
↓driveId [variables.RequestableVariable-1772719755348]: 
  comment: Optional drive identifier. If provided, driveName/listId/listName are ignored.
  description: Optional Graph drive identifier
↓driveName [variables.RequestableVariable-1772719755413]: 
  comment: Drive name used to resolve driveId when driveId is not provided.
  description: SharePoint drive name
  value: ${lib_Microsoft_Sharepoint.onPrem.driveName=TestLibrary}
↓listId [variables.RequestableVariable-1772719755486]: 
  comment: Optional list identifier used to resolve associated drive.
  description: Optional Graph list identifier
↓listName [variables.RequestableVariable-1772719755566]: 
  comment: List display name or internal name used to resolve associated drive.
  description: SharePoint list name
  value: ${lib_Microsoft_Sharepoint.onPrem.listName=TestList}
↓itemId [variables.RequestableVariable-1772719755652]: 
  comment: Target drive item identifier owning the permission to delete.
  description: Target drive item id
↓permissionId [variables.RequestableVariable-1772719755750]: 
  comment: Target permission identifier to delete.
  description: Target permission id
↓includeDeletedPermissionSnapshot [variables.RequestableVariable-1772719755854]: 
  comment: true reads permission before deletion and returns it in response.data.permission.
  description: Return deleted permission snapshot
  value: false
↓includeRawPermission [variables.RequestableVariable-1772719755965]: 
  comment: true includes raw Graph permission payload under permission.raw when snapshot is enabled.
  description: Include raw Graph payload
  value: false
↓provider [variables.RequestableVariable-1772719756082]: 
  comment: Backend provider selection graph or onprem.
  description: Provider selection graph or onprem
  value: ${lib_Microsoft_Sharepoint.provider}
↓accessToken [variables.RequestableVariable-1772719756206]: 
  comment: Optional delegated bearer token. If provided, tenant/client/secret are ignored.
  description: Delegated Microsoft Graph access token
↓tenantId [variables.RequestableVariable-1772719756338]: 
  comment: Azure Entra tenant id used for app-only token acquisition.
  description: Tenant id for app-only token
  value: ${Microsoft_AzGraph.tenantId}
↓clientId [variables.RequestableVariable-1772719756477]: 
  comment: Azure Entra application client id used for app-only token acquisition.
  description: Client id for app-only token
  value: ${Microsoft_AzGraph.clientId}
↓clientSecret [variables.RequestableVariable-1772719756625]: 
  comment: Azure Entra application client secret used for app-only token acquisition.
  description: Client secret for app-only token
  value: 
    - ↑ciphered: true
    - ↑traceable: false
    - →→: xa51e43ef1d108aa61557c46e9670abb3d50668c28091a8d24f75c1d2eb802780bc1e972ff7382dfe8bbbe42f37b74544
  visibility: '-1'
↓TC_DeleteDriveItemPermission_Graph [core.TestCase]: 
  ↓siteHostname [variables.TestCaseVariable-1772803982855]: 
    value: convertigo.sharepoint.com
  ↓sitePath [variables.TestCaseVariable-1772803982884]: 
    value: /sites/test1
  ↓driveName [variables.TestCaseVariable-1772803982919]: 
    value: Documents
  ↓itemId [variables.TestCaseVariable-1772803982962]: 
    value: 01PETTHVHBULYXIKX2EZFIAFNZVAYMGFUP
  ↓permissionId [variables.TestCaseVariable-1772803983021]: 
    value: dGVzdDEgVmlzaXRvcnM
  ↓includeDeletedPermissionSnapshot [variables.TestCaseVariable-1772803983077]: 
    value: false
  ↓provider [variables.TestCaseVariable-1772803983153]: 
    value: graph
↓TC_DeleteDriveItemPermission_OnPrem [core.TestCase]: 
  ↓siteHostname [variables.TestCaseVariable-1772803983580]: 
    value: ${lib_Microsoft_Sharepoint.onPrem.http.server=sharepoint.local}
  ↓sitePath [variables.TestCaseVariable-1772803983598]: 
    value: ${lib_Microsoft_Sharepoint.onPrem.sitePath=/sites/test}
  ↓driveName [variables.TestCaseVariable-1772803983632]: 
    value: ${lib_Microsoft_Sharepoint.onPrem.driveName=TestLibrary}
  ↓itemId [variables.TestCaseVariable-1772803983678]: 
    value: 01PETTHVHBULYXIKX2EZFIAFNZVAYMGFUP
  ↓permissionId [variables.TestCaseVariable-1772803983738]: 
    value: dGVzdDEgVmlzaXRvcnM
  ↓includeDeletedPermissionSnapshot [variables.TestCaseVariable-1772803983798]: 
    value: false
  ↓provider [variables.TestCaseVariable-1772803983878]: 
    value: onprem