accessibility: Hidden
authenticatedContextRequired: true
comment: Lists permissions of one SharePoint drive item through Microsoft Graph (online), with graceful fallback when unsupported on-prem. (Graph permissions Files.Read.All|Files.ReadWrite.All|Sites.Read.All|Sites.ReadWrite.All)
↓inputVars [steps.InputVariablesStep-1772719772904]: 
  comment: Exposes request inputs in XML for debugging and source mapping.
↓ExecuteListDriveItemPermissions [steps.SimpleStep-1772719772956]: 
  comment: Resolves site and drive then lists permissions on one drive item.
  expression: |
    'include("js/sharepoint_provider.js");
    include("js/sharepoint_graph_common.js");
    
    var graphOperation = "list_drive_item_permissions";
    var requiredPermission = "Files.Read.All|Files.ReadWrite.All|Sites.Read.All|Sites.ReadWrite.All";
    var tokenMode = "delegated_or_application";
    response = null;
    var requestedProvider = sppr_normalizeProvider(provider, "graph");
    
    if (requestedProvider === "onprem") {
    	tokenMode = "onprem";
    	response = gs_responseOk(graphOperation, tokenMode, requiredPermission, {
    		supported: false,
    		fallbackStatus: "onprem_not_supported",
    		message: "Listing drive item permissions is not supported for SharePoint on-prem in unified mode. Use provider=graph.",
    		query: {
    			itemId: gs_safeString(itemId),
    			top: gs_parseInteger("top", top, 200),
    			maxPages: gs_parseInteger("maxPages", maxPages, 20),
    			selectFields: gs_splitCsv(selectFields),
    			includeRawPermission: gs_toBoolean(includeRawPermission, false)
    		},
    		count: 0,
    		pagesFetched: 0,
    		hasMore: false,
    		nextLink: "",
    		permissions: []
    	});
    } else {
    	try {
    		var tokenInfo = gs_resolveToken(accessToken, tenantId, clientId, clientSecret);
    		tokenMode = tokenInfo.mode;
    
    		var targetItemId = gs_require("itemId", itemId);
    		if (String(targetItemId).toLowerCase() === "root") {
    			throw new java.lang.IllegalArgumentException("Listing permissions on drive root is not allowed. Provide a child item id.");
    		}
    
    		var selectedFields = gs_splitCsv(selectFields);
    		var pageTop = gs_parseInteger("top", top, 200);
    		if (pageTop < 1) {
    			pageTop = 1;
    		}
    		if (pageTop > 999) {
    			pageTop = 999;
    		}
    
    		var maxPageCount = gs_parseInteger("maxPages", maxPages, 20);
    		if (maxPageCount < 1) {
    			maxPageCount = 1;
    		}
    		if (maxPageCount > 100) {
    			maxPageCount = 100;
    		}
    
    		var includeRaw = gs_toBoolean(includeRawPermission, false);
    
    		var resolvedSite = gs_resolveSite(tokenInfo.token, siteId, siteHostname, sitePath);
    		var resolvedDriveData = gs_resolveDrive(tokenInfo.token, resolvedSite, driveId, driveName, listId, listName);
    		var resolvedDrive = resolvedDriveData.drive;
    		var resolvedList = resolvedDriveData.list;
    
    		var requestInfo = gs_graphDriveItemPermissionsGetRequestInfo(tokenInfo.token, resolvedDrive.driveId, targetItemId);
    		gs_graphAddQueryParameter(requestInfo, "$top", String(pageTop));
    		if (selectedFields.length > 0) {
    			gs_graphAddQueryParameter(requestInfo, "$select", selectedFields.join(","));
    		}
    
    	var responsePermissions = [];
    	var nextUrl = gs_requestInfoUrl(requestInfo);
    	var pageResult = gs_graphCollectPagedValues(nextUrl, tokenInfo.token, maxPageCount);
    	var permissionCandidates = pageResult.values;
    	var pageCount = pageResult.pagesFetched;
    	nextUrl = pageResult.nextLink;
    
    	var p;
    	for (p = 0; p < permissionCandidates.length; p++) {
    		responsePermissions.push(gs_normalizePermission(permissionCandidates[p], includeRaw));
    	}
    
    		response = gs_responseOk(graphOperation, tokenMode, requiredPermission, {
    			siteId: resolvedSite.siteId,
    			siteHostname: resolvedSite.siteHostname,
    			sitePath: resolvedSite.sitePath,
    			driveId: resolvedDrive.driveId,
    			driveName: resolvedDrive.driveName,
    			driveWebUrl: resolvedDrive.driveWebUrl,
    			driveType: resolvedDrive.driveType,
    			sharepointIds: resolvedDrive.sharepointIds,
    			quota: resolvedDrive.quota,
    			owner: resolvedDrive.owner,
    			resolvedFromList: resolvedList !== null,
    			listId: resolvedList === null ? "" : resolvedList.listId,
    			listName: resolvedList === null ? "" : resolvedList.listName,
    			listDisplayName: resolvedList === null ? "" : resolvedList.listDisplayName,
    			listWebUrl: resolvedList === null ? "" : resolvedList.listWebUrl,
    			listInfo: resolvedList === null ? {} : resolvedList.listInfo,
    			listSharepointIds: resolvedList === null ? {} : resolvedList.sharepointIds,
    			query: {
    				itemId: targetItemId,
    				top: pageTop,
    				maxPages: maxPageCount,
    				selectFields: selectedFields,
    				includeRawPermission: includeRaw
    			},
    			count: responsePermissions.length,
    			pagesFetched: pageCount,
    			hasMore: !gs_isBlank(nextUrl),
    			nextLink: gs_safeString(nextUrl),
    			permissions: responsePermissions
    		});
    	} catch (e) {
    		response = gs_responseError(graphOperation, tokenMode, requiredPermission, e);
    	}
    }'
↓response [steps.JsonToXmlStep-1772719772988]: 
  comment: Converts the JS response object into XML output.
  jsonObject: 
    - xmlizable: 
      - ↑classname: com.twinsoft.convertigo.beans.steps.SmartType
      - SmartType: 
        - ↑mode: JS
        - →→: response
  key: 
    - xmlizable: 
      - ↑classname: com.twinsoft.convertigo.beans.steps.SmartType
      - SmartType: 
        - ↑mode: PLAIN
        - →→: response
↓siteId [variables.RequestableVariable-1772719773029]: 
  comment: Optional site identifier. If empty, siteHostname and sitePath are used to resolve it.
  description: Optional Graph site identifier
↓siteHostname [variables.RequestableVariable-1772719773078]: 
  comment: SharePoint hostname when resolving siteId, for example contoso.sharepoint.com.
  description: SharePoint hostname
  value: ${lib_Microsoft_Sharepoint.onPrem.http.server=sharepoint.local}
↓sitePath [variables.RequestableVariable-1772719773133]: 
  comment: Site path when resolving siteId, for example /sites/engineering.
  description: SharePoint site path
  value: ${lib_Microsoft_Sharepoint.onPrem.sitePath=/sites/test}
↓driveId [variables.RequestableVariable-1772719773253]: 
  comment: Optional drive identifier. If provided, driveName/listId/listName are ignored.
  description: Optional Graph drive identifier
↓driveName [variables.RequestableVariable-1772719773341]: 
  comment: Drive name used to resolve driveId when driveId is not provided.
  description: SharePoint drive name
  value: ${lib_Microsoft_Sharepoint.onPrem.driveName=TestLibrary}
↓listId [variables.RequestableVariable-1772719773446]: 
  comment: Optional list identifier used to resolve associated drive.
  description: Optional Graph list identifier
↓listName [variables.RequestableVariable-1772719773550]: 
  comment: List display name or internal name used to resolve associated drive.
  description: SharePoint list name
  value: ${lib_Microsoft_Sharepoint.onPrem.listName=TestList}
↓itemId [variables.RequestableVariable-1772719773678]: 
  comment: Target drive item identifier.
  description: Target drive item id
↓top [variables.RequestableVariable-1772719773797]: 
  comment: Maximum number of permissions requested per Graph page.
  description: Page size
  value: 200.0
↓maxPages [variables.RequestableVariable-1772719773919]: 
  comment: Maximum number of Graph pages fetched before stopping pagination.
  description: Maximum pages
  value: 20.0
↓selectFields [variables.RequestableVariable-1772719774043]: 
  comment: Optional comma-separated list of Graph permission properties to select.
  description: Comma-separated property selection
↓includeRawPermission [variables.RequestableVariable-1772719774175]: 
  comment: true includes raw Graph permission payload under response.data.permissions[*].raw.
  description: Include raw Graph permission payload
  value: false
↓provider [variables.RequestableVariable-1772719774317]: 
  comment: Backend provider selection graph or onprem.
  description: Provider selection graph or onprem
  value: ${lib_Microsoft_Sharepoint.provider}
↓accessToken [variables.RequestableVariable-1772719774475]: 
  comment: Optional delegated bearer token. If provided, tenant/client/secret are ignored.
  description: Delegated Microsoft Graph access token
↓tenantId [variables.RequestableVariable-1772719774631]: 
  comment: Azure Entra tenant id used for app-only token acquisition.
  description: Tenant id for app-only token
  value: ${Microsoft_AzGraph.tenantId}
↓clientId [variables.RequestableVariable-1772719774793]: 
  comment: Azure Entra application client id used for app-only token acquisition.
  description: Client id for app-only token
  value: ${Microsoft_AzGraph.clientId}
↓clientSecret [variables.RequestableVariable-1772719774958]: 
  comment: Azure Entra application client secret used for app-only token acquisition.
  description: Client secret for app-only token
  value: 
    - ↑ciphered: true
    - ↑traceable: false
    - →→: xa51e43ef1d108aa61557c46e9670abb3d50668c28091a8d24f75c1d2eb802780bc1e972ff7382dfe8bbbe42f37b74544
  visibility: '-1'
↓TC_ListDriveItemPermissions_Graph [core.TestCase]: 
  ↓siteHostname [variables.TestCaseVariable-1772803994122]: 
    value: convertigo.sharepoint.com
  ↓sitePath [variables.TestCaseVariable-1772803994148]: 
    value: /sites/test1
  ↓driveName [variables.TestCaseVariable-1772803994183]: 
    value: Documents
  ↓itemId [variables.TestCaseVariable-1772803994228]: 
    value: 01PETTHVHBULYXIKX2EZFIAFNZVAYMGFUP
  ↓top [variables.TestCaseVariable-1772803994289]: 
    value: 100.0
  ↓maxPages [variables.TestCaseVariable-1772803994349]: 
    value: 5.0
  ↓provider [variables.TestCaseVariable-1772803994425]: 
    value: graph
↓TC_ListDriveItemPermissions_OnPrem [core.TestCase]: 
  ↓siteHostname [variables.TestCaseVariable-1772803994856]: 
    value: ${lib_Microsoft_Sharepoint.onPrem.http.server=sharepoint.local}
  ↓sitePath [variables.TestCaseVariable-1772803994875]: 
    value: ${lib_Microsoft_Sharepoint.onPrem.sitePath=/sites/test}
  ↓driveName [variables.TestCaseVariable-1772803994909]: 
    value: ${lib_Microsoft_Sharepoint.onPrem.driveName=TestLibrary}
  ↓itemId [variables.TestCaseVariable-1772803994950]: 
    value: 01PETTHVHBULYXIKX2EZFIAFNZVAYMGFUP
  ↓top [variables.TestCaseVariable-1772803995009]: 
    value: 100.0
  ↓maxPages [variables.TestCaseVariable-1772803995068]: 
    value: 5.0
  ↓provider [variables.TestCaseVariable-1772803995142]: 
    value: onprem